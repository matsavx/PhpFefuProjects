{% extends 'base.html.twig' %}

{% block title %}Hello!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <header>
        <ul>
            <li {{ app.request.get('_route') == 'pizzaList' ? 'active' }}>
                <a href="{{ path('pizzaList') }}">Пиццы</a>
            </li>
            <li {{ app.request.get('_route') == 'drinkList' ? 'active' }}>
                <a href="{{ path('drinkList') }}">Напитки</a>
            </li>
            <li {{ app.request.get('_route') == 'rollList' ? 'active' }}>
                <a href="{{ path('rollList') }}">Роллы</a>
            </li>
            <li {{ app.request.get('_route') == 'kitList' ? 'active' }}>
                <a href="{{ path('kitList') }}">Корзина</a>
            </li>
        </ul>
    </header>
    <div class="example-wrapper">
        <h1>Hello</h1>
        <ul>

        </ul>
    </div>
{% endblock %}
